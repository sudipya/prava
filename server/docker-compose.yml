services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: prava
      POSTGRES_PASSWORD: change-me
      POSTGRES_DB: prava
    ports: ["5432:5432"]
    volumes: ["prava_pgdata:/var/lib/postgresql/data"]
volumes:
  prava_pgdata:
